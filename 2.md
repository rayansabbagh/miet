# 2  
  
t=np.linspace(0,2*np.pi,200000)  
r1=2*np.ones_like(t)  
r2=3*np.cos(3*t)  
  
ax=plt.subplot(111,projection='polar')  
ax.plot(t,r1)  
ax.plot(t,r2)  
plt.show()  
  
np.trapz(np.sqrt(r2**2+np.gradient(r2,t)**2)*(np.abs(r2)>2),t)  
  
  
3  
  
quad(lambda t: np.exp(-t**2/2)/np.sqrt(2*np.pi),1,14)[0]  
  
  
4  
  
4, next(n for n in range(1,100000) if 3/(n+1)+2**(-n)<1e-3)  
  
  
5  
  
[np.arctan(n) for n in [10,100,1000]]  
  
sum(((-1)**n)/(n*5**n) for n in range(1,100)), next(n for n in range(1,100) if 1/((n+1)*5**(n+1))<1e-4)  
  
  
6  
  
e=1e-4  
  
x=0  
while 1:  
    y=np.sqrt(np.arcsin(x/2))  
    if abs(y-x)<e: break  
    x=y  
x1=y  
  
x=1.5  
while 1:  
    y=np.sqrt(np.pi-np.arcsin(x/2))  
    if abs(y-x)<e: break  
    x=y  
x2=y  
  
[0,x1,x2]  
  
  
7  
  
e=1e-4  
x=np.zeros(3)  
  
while 1:  
    y=np.array([(-x[1]+x[2])/4,(x[0]+x[2]-1)/5,(-x[0]+x[1])/4])  
    if np.max(abs(y-x))<e: break  
    x=y  
  
