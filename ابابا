f=lambda x: x-2*np.sin(x**2)

x=0.5
while 1:
    y=2*np.sin(x**2)
    if abs(y-x)<1e-4: break
    x=y
x1=y

x=1.5
while 1:
    y=np.sqrt(np.pi-np.arcsin(x/2))
    if abs(y-x)<1e-4: break
    x=y
x2=y

[0,x1,x2]
