x = np.linspace(-5, 5, 201)
y = np.linspace(-5, 5, 201)

X, Y = np.meshgrid(x, y)
Z = 3 + 2*X - Y - X**2 + X*Y - Y**2

fig, ax = plt.subplots()
cs = ax.contour(X, Y, Z, levels=np.linspace(-20, 5, 20))
ax.clabel(cs, cs.levels, inline=True, fontsize=10)

plt.xlabel('x')
plt.ylabel('y')
plt.show()


def f(t):
    x, y = t
    return -(3 + 2*x - y - x**2 + x*y - y**2)


maximum, f_max, _, _, _ = fmin(f, np.array([1, 0]), xtol=1e-9, disp=False, full_output=True)

print('Max:', [f"{i:.2f}" for i in maximum], 'Function value at maximum:', -f_max)
