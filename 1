import numpy as np
import matplotlib.pyplot as plt

x0 = 1
y0 = 2
r = 0.3

R = r / (1 - r)

x = [x0 + R, x0, x0 - R, x0, x0 + R]
y = [y0, y0 + R, y0, y0 - R, y0]

plt.figure()
plt.fill(x, y, 'c')
plt.axis('equal')
plt.grid(True)
plt.xlim(x0 - 0.7, x0 + 0.7)
plt.ylim(y0 - 0.7, y0 + 0.7)
plt.show()
